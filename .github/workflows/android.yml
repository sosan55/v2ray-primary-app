name: Android APK Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build debug and release APKs
    runs-on: ubuntu-latest
    env:
      KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
      STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
      KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
      KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Remove corrupt gradle-wrapper.jar
        run: rm -f gradle/wrapper/gradle-wrapper.jar

      - name: Set up Gradle (to regenerate wrapper)
        uses: gradle/actions/setup-gradle@v4

      - name: Regenerate gradle-wrapper.jar
        run: gradle wrapper --gradle-version 9.3.1 --distribution-type bin

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Display environment (debug)
        run: |
          echo "KEY_ALIAS=${KEY_ALIAS}" && echo "STORE_PASSWORD is set: ${STORE_PASSWORD:+yes}"

      - name: Build Debug APK
        run: ./gradlew :app:assembleDebug --no-daemon --stacktrace

      - name: Build Release APK
        run: ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: apks
          path: |
            app/build/outputs/apk/debug/**/*.apk
            app/build/outputs/apk/release/**/*.apk
